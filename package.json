{
  "name": "ergonia-blog",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "description": "The human channel for Ergonia (ergonia.works). Static site, text first, no JS at read time.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "node scripts/build.mjs && node scripts/verify.mjs"
  },
  "devDependencies": {
    "marked": "^15.0.0",
    "wrangler": "^3.100.0"
  }
}
